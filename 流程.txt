MainPage里加一个Timer控件，这个控件的作用是一定时间会自动触发一次，用于定时换图
每次触发，就根据配置信息，确定是使用Top50还是PixivUser
(Pixiv类)具体操作从Top50和PixivUser里调用
HttpUtil是辅助Pixiv的
拿到图片后写入缓存，timer通知ShowPage从缓存取图
同时将图片设置成桌面
一次流程结束~
--------------------------------------------------
由于电脑没有后台被杀死的问题
所以队列就直接放内存
但是Cookie一类的也要写文件~